package com.example.appnumber29;

public interface LifeInsurance {

    // Interface is like a class but the interface is the collection of abstract methods!
    // Abstract method don't have any body, it declare where the interface implements!

     double evaluteCostOfinsurance();
}
